 {r}←Initial parms
 r←1
 ##.FilesAndDirs.PolishCurrentDir
 :If 0=parms.debugFlag
     ⎕←'******* Sorry, but for technical reasons this test suite'
     ⎕←'        can only be executed without error trapping'
     ⎕←'        and with the debug flag switched on!'
     ⎕←'        ===> Press <enter> to continue'
     {}⍞
     r←0
 :EndIf
