 r←Test_Run_02(stopFlag batchFlag);log;rc;stopFlag;⎕TRAP
⍝ Exercise the "RunBatch" method
 ⎕TRAP←(999 'C' '. ⍝ Deliberate error')(0 'N')
 r←T.∆Failed

 TestCasesSimu.T←⎕NEW ##.Tester2 TestCasesSimu
 TestCasesSimu.T.TestFlag←1
 (rc log)←TestCasesSimu.T.RunBatchTests ⍬
 TestCasesSimu.⎕EX'T'
 →T.PassesIf 4=+/'...not found'⍷∊log  ⍝ No function `Initial`, no INI file and no function `Cleanup`
 →T.PassesIf∨/'13 test cases executed'⍷∊log
 →T.PassesIf∨/'1 test case failed (flagged with "*")'⍷∊log
 →T.PassesIf∨/'2 test cases not executed because they are not "batchable" (flagged with "-")'⍷∊log

 r←T.∆OK
⍝Done
