 R←Test_ZZZ_997(stopFlag batchFlag);⎕IO;⎕ML;⎕TRAP;xml;report
⍝ Checks on whether the "publish.config" file carries a "<container>" tag.
 ⎕IO←0 ⋄ ⎕ML←3
 ⎕TRAP←(999 'C' '∘∘∘ ⍝ Deliberate error')(0 'N')
 R←T._Failed

 report←''
 xml←⎕XML↑##.FilesAndDirs.NGET'publish.config'
 →T.FailsIf~(⊂'container')∊xml[;1]

 R←T._OK
