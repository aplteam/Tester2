 r←Test_Misc_19(stopFlag batchFlag);⎕TRAP;rc;report
⍝ Check whether neither `Initial` nor `Cleanup` are executed in case no test function was executed.
 ⎕TRAP←(999 'C' '∘∘∘ ⍝ Deliberate error')(0 'N')
 r←T._Failed

 #.⎕SHADOW'TEMP'
 'TEMP'#.⎕NS''
 #.TEMP.T←⎕NEW ##.Tester2 #.TEMP
 #.TEMP.(∆INITIAL ∆CLEANUP)←0
 #.TEMP.⎕FX'Initial' '∆INITIAL←1'
 #.TEMP.⎕FX'Cleanup' '∆CLEANUP←1'
 #.TEMP.T.TestFlag←1
 (rc report)←#.TEMP.T.Run 1
 →T.PassesIf 0=rc
 →T.PassesIf 0=+/#.TEMP.(∆CLEANUP ∆INITIAL)
 (rc report)←#.TEMP.T.RunThese''
 →T.PassesIf 0=rc
 →T.PassesIf 0=+/#.TEMP.(∆CLEANUP ∆INITIAL)

 r←T._OK
⍝Done
