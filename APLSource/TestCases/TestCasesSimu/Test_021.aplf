 r←Test_021(debugFlag batchFlag);⎕TRAP
⍝ Broken
⍝ Note that when the function executing this test case does not trap errors then
⍝ this test case is reported as "failed" rather than "broken.
 r←T._Failed
 ∘∘∘  ⍝ Now enter "→0" when running the #.Tester2 test cases
